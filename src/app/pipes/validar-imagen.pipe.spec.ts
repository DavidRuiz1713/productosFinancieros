import { ValidarImagenPipe } from './validar-imagen.pipe';

describe('ValidarImagenPipe', () => {
  it('create an instance', () => {
    const pipe = new ValidarImagenPipe();
    expect(pipe).toBeTruthy();
  });
});
