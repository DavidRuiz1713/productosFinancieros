import { LetraInicialPipe } from './letra-inicial.pipe';

describe('LetraInicialPipe', () => {
  it('create an instance', () => {
    const pipe = new LetraInicialPipe();
    expect(pipe).toBeTruthy();
  });
});
